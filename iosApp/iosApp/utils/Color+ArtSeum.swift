import SwiftUI

extension Color {
    static let artSeumBackground = Color(hex: 0x0B0B0B)
    static let artSeumAccent = Color(hex: 0x895323)
    static let artSeumSecondaryText = Color(hex: 0xAFAFAF)
    static let artSeumSeparator = Color(hex: 0x3A3A3A)
}
